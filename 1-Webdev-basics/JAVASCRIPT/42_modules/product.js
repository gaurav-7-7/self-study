// same method name as in index.js but this will override the method and its function will be performed
export var mul = (name, price) => {
    return{
        name : name,
        price : price
    }
};
